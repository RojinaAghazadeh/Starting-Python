# Here is how to cut a list
numbers = ['one','two','three','four','five','six']
print(numbers[0:3])

# and without starting index, python starts at the beginning.
print(numbers[:4])

# the reverse form, starts from the end of the list.
print(numbers[1:])
